import type { Metadata } from 'next'
import Spacebar from '@/components/Spacebar'

export const metadata: Metadata = {
  title: 'Spacebar | INGLU',
}

export default function SpacebarPage() {
  return <Spacebar />
}
